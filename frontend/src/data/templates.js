// Templates are authoritative on the backend (/api/templates).
// This export remains only as an empty compatibility surface for older builder imports.
// New template discovery and import must go through templateService.
export const templates = []
